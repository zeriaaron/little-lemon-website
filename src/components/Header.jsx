import logo from '../assets/images/Logo.svg'

const Header = () => {
    return (
        <header>
            <img src={logo} alt="Little Lemon Logo" />
        </header>
    )
}

export default Header